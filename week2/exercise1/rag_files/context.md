# JaiChat project notes

JaiChat is a local AI chat application built with FastAPI, vanilla HTML, CSS and JavaScript.

It uses Ollama through an OpenAI-compatible API.

The app supports streaming responses, SQLite conversation storage, previous chat selection and automatic conversation titles.

The assistant is called Jai.

The next improvement is Simple RAG, where Jai answers using a local context file.
